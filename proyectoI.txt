1) Crear una Red Docker:
    #docker network create my_network //// PELIGRO

2) Ejecutar un Contenedor para postgresql
    docker run -d --name db_postgres_proyectoi --network my_network -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=01092003 -e POSTGRES_DB=sistema_de_inscripcion -v postgres_data:/var/lib/postgresql/data -p 5433:5432 postgres:16

    ** Con la Red interna de Dokcer
    docker run -d --name db_postgres_proyectoi -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=01092003 -e POSTGRES_DB=sistema_de_inscripcion -v postgres_data:/var/lib/postgresql/data -p 5433:5432 postgres:16

    **Ultimo intento:
    docker run -d --name db_postgres_proyecti -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=01092003 -e POSTGRES_DB=sistema_de_inscripcion -v postgres_data:/var/lib/postgresql/data -p 5433:5432 postgres:16



3) Contruir una Imagen para el Backend    
    #docker build -t backend_image_proyectoi ./backend
    #docker run -d --name backend_proyectoi --network my_network -e DB_USER=postgres -e DB_HOST=host.docker.internal -e DB_DATABASE=sistema_de_inscripcion -e DB_PASSWORD=01092003 -e DB_PORT=5432 -p 5000:5000 backend_image_proyecti 

    ** Con la Red interna de Dokcer
    docker run -d --name backend_proyectoi -e DB_USER=postgres -e DB_HOST=host.docker.internal -e DB_DATABASE=sistema_de_inscripcion -e DB_PASSWORD=01092003 -e DB_PORT=5433 -p 5000:5000 backend_image_proyecti

    **Ultimo intento
    docker run -d --name backend_proyecti --link db_postgres_proyecti -e DB_USER=postgres -e DB_HOST=db_postgres_proyecti -e DB_DATABASE=sistema_de_inscripcion -e DB_PASSWORD=01092003 -e DB_PORT=5432 -p 5000:5000 backend_image_proyecti

4) Contruir una Imagen para el Frontend
    #docker build -t frontend_image_proyectoi ./frontend
    #docker run -d --name frontend_proyectoi --network my_network -p 80:80 frontend_image_proyecti

    ** Con la Red interna de Dokcer
    docker run -d --name frontend_proyectoi -p 80:80 frontend_image_proyecti

    **Ultimo intento
    docker run -d --name frontend_proyectoi --link backend_proyecti -p 5573:80 frontend_image_proyectoi



**Parar y Eliminar un Contenedor
    **Listar Contenedores Activos
        #docker ps
    **Para el Contenedor
        #docker stop backend_proyectoi
    **Eliminar el Contenedor
        #docker rm backend_proyectoi

